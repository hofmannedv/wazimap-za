$(function() {
  // add announcement banner
  var banner = $('<div id="banner"><div class="wrapper">Wazimap now uses the latest boundaries from the 2016 Municipal Elections. <a href="#">Find out more</a>.</div></div>');
  $('#page-header').append(banner);
});
